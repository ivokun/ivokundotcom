[install]
# Pin the registry explicitly (Effect packages use subpath exports that
# need consistent resolution across workspaces)
registry = "https://registry.npmjs.org"

[resolve]
# Effect packages use subpath exports; keep Bun resolving them consistently
conditions = ["import", "module", "node"]
